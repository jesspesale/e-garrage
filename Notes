Spring #1
User Functionality
    /register
    /login
    /logout

User
    authrntication
    email/password
    unique emails
 has many recipes

 Sprint #2
 Basic Recipe 
  New/ create
  Index
  Show

Sprint #3
Editing recipes
  have a recipe page display all the traits of a single recipe
  
  <input type="text" name="ingredients">
  <div><label for="instructions">Instructions: </label><textarea name="instructions" id="instructions" rows="20" cols="80" required ></textarea></div>



get '/users'            index page to display all users w/ link to their profile (and amybe some recipe names)
    erb :'/users/show'
end

get '/users/:id'           A users a users profile - name, recipes names w/ links to recipe page

end

get '/recipes/new' do       the form for creating a new recipe
    erb :'/recipes/new'
end

post '/recipes' do
    @recipe = Recipe.create(name: params[:name]), ingredients: params[:ingredients]...)

    redirect to '/recipes/
end 

How to submit a list of items in an html form?


Need to capatilize the first word of each instruction?
<% @instructions.collect! do |inst| %>
    <% inst.capitalize %> 
     <% end %>



    <% if logged_in? %>
    Logged in as: <%= current_user.email %>
      <a href="/logout">Logout</a>
      <% else %>
      <a href="/signup">Sign Up</a>
      <a href="/login">Log in</a>
      <% end %>
